# D/LIGHT GYM 体験トレーニング LP

八丁堀・新富町のパーソナルジム「D/LIGHT GYM」の**体験トレーニング申込に特化した1枚完結のランディングページ**。
広告・SNS・チラシQRなどからの流入先を想定しています。

本家サイト（`../ホームページ作成/site/`）の内容とデザインをもとに作成していますが、
**CSS・JS・画像まで含めてこのフォルダだけで完結する自己完結型**です。本家を編集しても、このLPには影響しません（逆も同じ）。

## プレビュー

```bash
python3 serve.py
```

→ http://localhost:4322 （停止は Control + C）

## 構成

**本番は `index.html`（B案）。** 2026年8月に、反応型LPの構成を参考にしたB案へ差し替えた。
`lp-a.html` は差し替え前のA案で、比較用に残してある（本番URLを名乗らないよう canonical は外してある）。

```
LP作成/
├── index.html          LP本体＝B案（本番）
├── lp-a.html           A案（差し替え前。比較用に保存）
├── compare.html        A案とB案を並べて見比べるページ
├── serve.py            ローカルプレビュー用サーバー
└── assets/
    ├── css/lp-b.css    B案（本番）のデザイン。冒頭 :root に色の変数
    ├── css/lp.css      A案のデザイン
    ├── js/lp-b.js      B案の挙動
    ├── js/lp.js        A案の挙動（ヘッダー透過切替／スクロール表示／FAQ開閉／固定CTA）
    ├── js/analytics.js GA4計測（測定IDを入れるまで何も送信しません）
    └── img/            写真（本家から流用し、LP用に軽量化済み）
```

## ページ構成（上から順に）

証拠 →なぜ変わるか →自分と同じ人がいる →手順 →人 →声 →価格 →疑問、の順に組んでいる。

1. ヒーロー — 「今年こそは健康に」／残り◯ヶ月、今年は何回考えましたか。／どうせやるなら今から
   → 気づき3つ → 予防投資 → 店名 → 実績2つ
   ＋体験80分5,000円＋CTA2種（予約／LINE）＋安心材料5点
   **原稿はオーナー自身が書いたもの。言い回しも句読点も勝手に足さないこと。**
   訴求の軸は「悪くなってから直す」ではなく「動ける今から始める予防投資」。
   なお、予防の訴求は「まだ時間がある」とも読めて、そのままだと今日の行動につながりにくい。
   その橋渡しの3行は**最終CTAの見出しにだけ**置いている（ヒーローにも入れたが施主の指示で削除）。
2. お悩み6つ（`#trouble`／痛み・続かない・仕事のパフォーマンス・老後の不安）
3. **ビフォーアフター**（`#results`／主役1件＋3件。本家 `#results` から転記）
4. 選ばれる3つの理由＋コンセプト（`#reason`）
5. こんな方が通っています（`#members`／年代・入会時の経験・目的の円グラフ）
6. プログラム3種（`#program`／トレーニング／ストレッチ／**ゴルフ**。ピラティスは2026年8月に掲載終了）
   ＋末尾に**オーダーメイド帯**（`.made`／紺地。「4つのどれを選ぶのか」ではなく「毎回あなたに合わせる」と受け止める役割）
7. **こんな方におすすめです**（`#recommend`／チェック8項目＋「向かない方」の一文）
8. **体験の詳細＋入会金無料バナー＋6ステップの流れ＋持ち物**（`#trial` = 主要CV地点）
9. トレーナー（`#trainer`／代表・奥野雄大のみ。2026年8月に2人目を非掲載化）
10. お客様の声3件（`#voice`／40代男性・50代男性ゴルフ・50代女性）
11. 料金（`#price`）— 「料金を見る前に3つだけ」→ **初月のお支払い例**（入会金無料の適用後）→ 体験／都度払い（入会金なし） → 支払い・キャンセル → 予約CTA
12. FAQ 10問（`#faq`／アコーディオン。JSON-LDの FAQPage と件数を一致させること）
13. アクセス（`#access`／地図・営業時間）
14. 最終CTA — 見出しは**背中を押す3行**（`.cta__ttl`／明朝で大きく、最後の一行だけ赤）。
    本気で変わるなら早い方がいい／どうせやるなら今から／後からは後悔しかない。
    以前の「まずは80分、身体を預けてみてください。」は2026年8月に置き換えた。
    ＋入会金無料バナー ＋ フッター
15. スマホ固定CTAバー（ヒーロー高さの60%を過ぎたら出現）

### 2026年8月の全面改稿でやったこと

本家HPの更新（ビフォーアフター新設・料金の可視化）と、マーケティング／UI観点のレビューを反映した。

- **ビフォーアフター4件を新設**。証拠を「体験の案内」より前に置き、共感→証拠→理由の順にした
- **料金の透明化** — 都度払いは「入会金なし」と明示、初月総額を計算して提示。「あとから費用が出てくる」不信を消すため
- **指名料の表は施主の指示により非掲載**（2026年8月）。金額を並べると体験前の心理的ハードルになるため、代わりに「総額は体験時にその場でお出しします」に寄せている。
  そのため料金セクションとFAQでは**「追加費用はない」と言い切らず「基本的に」を必ず残すこと**（指名料自体は実在するため）
- **料金セクションの末尾に予約CTAを設置**。以前は本家サイトへの外部リンクしか出口がなく、`analytics.js` の `price_block` イベントが構造上ゼロだった
- お客様の声を1件→3件（ゴルフの声を追加。八丁堀の40代男性はゴルフ層と重なる）
- FAQに支払い方法・縛り・追加費用・仕事帰りの4問を追加（すべて本家の実データ）
- ヒーローのh1を最終的に**「運動は10年ぶり。それでも、体は変えられる。」**に変更。
  マッサージ前提のコピーだと通院習慣のない層を外すため、「自分にはもう無理だ」という障壁のほうを狙った。
  年齢を書かずに「10年ぶり」で30〜50代を想起させ、上の年代も弾かない
- **ファーストビューの証拠写真とビフォーアフターの主役を、どちらも50代男性の姿勢改善で統一**。訴求の軸をぶらさないため
- **オーダーメイド帯（`.made`）と「こんな方におすすめです」（`#recommend`）を新設**。
  プログラム3種を見せたあとの「で、自分はどれ？」を受け止め、体験セクションへ渡す導線にした。
  `#recommend` の末尾は施主の文言で「リバウンドをしない適切な痩せ方を提案する／急激なダイエットは推奨しない」と明示している
- 朝8時から営業していることをヒーローとFAQで訴求（仕事前に通える差別化）
- スマホでヒーローの予約ボタンがファーストビューに入るよう、`order` で価格の直後に配置
- 全画像をWebP化（3.0MB→1.8MB）。OGP用に `hero-home.jpg` のみJPGを残置
- JS未実行でも本文が消えないよう `<noscript>` でフォールバックを追加
- スマホ固定CTAの出現位置を固定420pxからヒーロー高さ基準に変更

### 本家HPと同期が必要なもの

このLPは自己完結型だが、**画像と実データは本家のコピー**なので、本家が更新されたら次を確認する。

```bash
# 共通で使っている画像に差分がないか
for f in assets/img/*.webp; do
  h="../ホームページ作成/site/assets/img/$(basename $f)"
  [ -f "$h" ] && ! cmp -s "$f" "$h" && echo "DIFF $(basename $f)"
done
```

- 2026/08/10 の本家コミット `4c2fc36` で**WebPのEXIF回転バグが修正された**。LP側も
  `pilates.webp`（横倒しだった）とビフォーアフター4枚を本家から再コピーして同期済み。
  `pilates` は縦長（1066×1600）になったので `index.html` の `width` / `height` 属性も直した。
- 同コミットで**ビフォーアフター20代男性が「セッション1回」→「2ヶ月の変化」に訂正**された。LP側も追随済み。
  数字を伴う実績は本家が一次情報なので、勝手に盛らない・独自に言い換えないこと。
- `golf-man` / `tired-man` / `training-man` は施主から直接受け取ったLP専用画像で、本家には無い（向きは確認済み）。
- ゴルフの写真は 2026/08/30 に `golf-medball.webp`（メディシンボールでのランジ）へ差し替えた。
  **`golf-man.webp` は消さないこと**。A案（`lp-a.html`）がまだ参照している。A案は比較用に
  当時のまま残す約束なので、共用の画像ファイルを上書きせず、B案だけ新ファイルに向けている。
  `.prog img` は `aspect-ratio:4/3` + `object-fit:cover` なので、元の 1068×671 のままだと
  ブラウザ側で左右が切れる。切れ方を固定するため、あらかじめ中央で 895×671（4:3）に
  切ってから書き出している（左の椅子と右のダンベルが落ちるだけで、人物は両方とも残る）。

## 広告を回すときの準備（未実施）

### 1. 計測ID（設定済み）

`assets/js/analytics.js` 冒頭の `GA_MEASUREMENT_ID` に `G-LESJ5MJNHV` を設定済み。
GA4スニペットを HTML の `<head>` に貼ってはいけない（このファイルが読み込むので二重計測になる）。

**`positionOf()` を触るときの注意。** この関数が `reserve_click` / `generate_lead` の
`position` を決めている。A案とB案でラッパーのクラス名が違う（A: `.header` `.hero`
`.cta-band` `.footer` ／ B: `.hd` `.fv` `.cta` `.ft`）ので、**両方を書かないと
書いていないほうのLPでCTAの内訳が取れなくなる。**
実際に2026年8月にこれが起きていて、B案10個のCTAのうち6個が `body` に落ちていた。
どれにも当てはまらないときは、囲っている `section` の id をそのまま使う。

```
確認方法：ブラウザのコンソールで
document.querySelectorAll('a[href*="book.squareup.com"]').length
と、GA4のDebugViewで position が全部バラけているかを見る
```


### Google広告のコンバージョンタグ（枠だけ用意済み・未設定）

`assets/js/analytics.js` 冒頭の `ADS_CONVERSION_ID` と `ADS_CONVERSION_LABEL` に貼れば動く。
**両方そろうまでは広告へ何も送らない**ので、空のまま置いておいて構わない。

Google広告 →「目標」→「コンバージョン」→ 対象のアクション →「タグを設定する」に出る
`send_to: 'AW-123456789/AbC-D_efGhIjKlMn'` を、スラッシュの前後で2つに割って入れる。

**gtag.js は1本しか読み込まないこと。** 広告用にもう1本 `<script>` を貼ると
同じイベントが二重に飛ぶ。GA4と広告は、1本のライブラリ＋2つの `config` で送り分けている。

**`gtag('config', 'AW-…')` は消さないこと。** これが無いと広告クリックの `gclid` が
Cookieに残らず、クリックとコンバージョンが結び付かない。
タグは動いているのにコンバージョン0件、という一番気づきにくい壊れ方をする。

動作確認済み（2026/08/30）：

| 設定 | 送信される `config` | 予約ボタンを押したとき |
|---|---|---|
| 空のまま | `G-LESJ5MJNHV` のみ | `reserve_click` / `generate_lead` のみ |
| ID＋ラベルあり | `G-…` と `AW-…` の2つ | 上記＋ `conversion`（`send_to: AW-…/ラベル`） |

**金額は送っていない。** この時点で分かるのは「予約ページを開いた」ことだけで、
体験の5,000円が入ったわけではない。仮の金額を送ると、その数字で入札が最適化されてしまう。
値を入れるならGoogle広告のコンバージョンアクション側で「デフォルト値」として設定する。

**このコンバージョンは「予約完了」ではなく「予約ページを開いたクリック」。**
実際の予約はSquare（`book.squareup.com`＝別ドメイン）で起きるので、このLPからは見えない。
Squareの管理画面にトラッキングコードを貼る欄があるかどうかが分かるまで、
この数字は**実際の予約より多く出る**前提で見ること。

### 2. UTM の付け方

LP側は、着地URLの `utm_*` / `gclid` / `fbclid` を **Square の予約URLに自動で引き継ぐ**（`assets/js/lp.js` の末尾）。
あわせて `lp_cta`（hero / results / trial / price / cta_band / mobile_bar など）も付くので、
「どの広告の、どのボタンから予約されたか」まで追える。

広告のリンク先URLの例：

```
https://dlightgym.com/lp/trial/?utm_source=google&utm_medium=cpc&utm_campaign=trial_2608_pain&utm_content=headline_a
```

命名は次で統一する。

- `utm_source`：`google` / `facebook` / `instagram` / `flyer` / `line`
- `utm_medium`：`cpc`（広告）/ `qr`（チラシ）/ `profile`（SNSプロフィール）
- `utm_campaign`：`trial_年月_訴求`（例 `trial_2608_pain`、`trial_2608_golf`）
- `utm_content`：クリエイティブの識別名

**要確認：** Square が未知のクエリパラメータを受け取っても予約ページが正常に開くか、実機で1回試すこと。
開かない場合は `lp.js` 末尾のブロックを削除すれば元に戻る。

### 3. 検索広告のキーワード案（下書き・要精査）

意図が強い順。ローカル検索は成約率が桁違いに高い。

- `八丁堀 パーソナルジム` / `新富町 パーソナルジム` / `茅場町 パーソナルジム` / `京橋 パーソナルジム`
- `中央区 パーソナルトレーニング` / `八丁堀 ジム 体験`
- `八丁堀 ストレッチ` / `パーソナルストレッチ 中央区`
- 除外キーワード：`求人` `バイト` `24時間` `無料` `セルフ`

広告文の下書き（見出し30字・説明90字以内）：

- 見出し：`八丁堀・新富町 パーソナルジム` ／ `体験80分5,000円 完全個室` ／ `整えてから鍛える`
- 説明：`肩こり・腰痛の根本改善から。ストレッチで整えてから鍛えるので痛みが出にくい。手ぶらでOK、無理な勧誘なし。八丁堀駅・新富町駅から徒歩5分、朝8時〜夜22時。`

### 4. Googleビジネスプロフィール

本家サイトの地図は GBP（`cid=6342977323958173212`）に紐付け済み。LPの「お客様の声」からもクチコミページへリンクしている。
**クチコミの平均点と件数が分かれば、LPに星として表示する**（数値は実データのみ。教えてもらうまで載せない）。

## 入会金無料キャンペーン

**内容は2つ。**

1. **入会金 33,000円（税込）→ 0円（無料）** ／ このページからのご予約限定
   （2026年8月に半額 16,500円から無料へ変更。同月「毎月5名様限定」は施主の指示で削除）
2. **体験当日にご入会の場合、体験料 5,000円（税込）が無料**

掲載箇所は次の4か所。**終了・変更するときは4か所すべてを直すこと**（`.campaign` で検索できます）。

1. ヒーロー（体験料金ボックスの隣）
2. 体験セクション `#trial` の体験カード直下
3. 料金セクション `#price` の「入会金」表（通常価格・適用条件を明記）
4. 最終CTA

あわせて `<meta name="description">` と `og:description` にもキャンペーン文言が入っています。

景表法（二重価格表示）の観点から、**通常価格 33,000円と適用条件「このページからのご予約限定」は必ず併記**したまま運用してください。条件を落として「入会金無料」だけを出さないこと。
人数の限定を外したので、**いま無料を支えている条件はこの一つだけ**です。
このページ以外から来た方には実際に 33,000円を申し受けている、という運用が前提になります。
**入会金無料と体験料無料を重ねると、合計 38,000円分の割引**です。

## CSS・JSを直したあとに必ずやること

スマホのブラウザは CSS を強くキャッシュするため、ファイルを直しても
古いままの見た目が残る（実際にSafariで発生した）。
**`index.html` と `lp-a.html` の `?v=` の日付を必ず更新すること。**

```bash
# 例：?v=20260827a → ?v=20260828a に一括置換
sed -i '' 's/?v=20260827a/?v=20260828a/g' index.html lp-a.html
```

## FAQ を増やす・減らす・書き換えるとき

`index.html` の `<head>` にある **FAQPage の構造化データも、同じ数・同じ文面に直すこと。**
Googleは「ページに表示されていない内容」が書かれた構造化データを無視するか、
悪質な場合は手動対策の対象にする。現在は7問で一致している。

`lp-a.html` にも別に10問ぶんの FAQPage が入っているが、こちらは比較用に残しているだけ。

## 見出しの「残り◯ヶ月」

ヒーローの見出しにある数字は、`lp-b.js` が**今日の日付から入れ直している**（`[data-months-left]`）。
手で書いたままだと月をまたいだ瞬間に嘘になるため。当月は数えない（8月なら9〜12月で4ヶ月）。
12月だけは0にせず1と出す。JSが動かない場合はHTMLに書いてある `4` がそのまま出る。

**この見出しは年始がもっとも効く。** Google広告を回すなら1月に予算を寄せる価値がある。

## 文字サイズ

読み手が40〜50代なので、**2026年8月に全体を1px上げている**（`lp-b.css` の
`font-size` 93宣言すべて。`clamp()` は最小・最大の両方を+1px）。
本文は15px→16px。これより小さくしないこと。

上げたことで狭い端末のヘッダーが伸びるため、`@media(max-width:360px)` のヘッダーCTAを
一緒に調整している（3行に割れるので、ここだけ字を戻して1行に）。
ヘッダーが伸びたぶんファーストビューを下げる処理は、`--hd-h` の実測に移した（後述）。

## ファーストビューの写真

**2026/08/30に、切り抜き2枚の構成から、本家HPと同じ「斜めに切った写真パネル」へ変えた。**
本家のトップ（`../ホームページ作成/site/index.html`）が同日に指導写真へ刷新されたのに合わせ、
LPを開いてから本家を見た人が同じ店だと分かるようにするため。

- 写真は本家と**同じファイル**（`hero-coaching.webp`／1024×1536）をコピーしている。
  本家が差し替えたら、ここも差し替える。
- 金のくさび（`.fv__wedge`）の色は本家の `#C6A667 → #D8C8A8` をそのまま使っている。
  B案の `--gold`（`#F5A524`）はオレンジ寄りで、本家と並べたとき別ブランドに見える。
- 切り抜き版（`hero-cutout.webp` / `hero-cutout-stretch.webp`）と `tools/cutout.swift` は
  **消していない**。戻したくなったときのために残してある。使う手順は git 履歴の
  「ファーストビューの写真」節（`c8d5b95` 以前）を参照。

### 枠がほぼ正方形（20/19）である理由

**「ラットプルダウンをしている」と分かるのに必要な範囲が、縦に長いから。**

写真（1024×1536）の中で要るのは **縦18%（トレーナーの頭の上）〜縦76%（お客様が
バーを握る両手）の58%**。ここが入らないと、バーが切れて「ただ座っている写真」になる。

枠は横長、写真は縦位置なので、`cover` で入れると**左右は全部入り、上下だけが切れる**。
枠に残る縦の割合は次の式で決まる。

```
残る割合 = (枠の高さ ÷ 枠の幅) × (1024 ÷ 1536)
        = (枠の高さ ÷ 枠の幅) × 0.667
```

58%を残すには **高さが幅の0.87倍以上**要る。つまり**横長の枠では物理的に入らない。**
`aspect-ratio:20/19`（高さ＝幅の95%）にして、縦16.1%〜79.5%が残るようにしている。

比が固定なので、**どの画面幅でも写り方がまったく同じ**になる。実測：

| 画面幅 | 枠 | 残る範囲 |
|---|---|---|
| 320px | 320×304 | 16.1〜79.5% |
| 390px | 390×371 | 16.1〜79.5% |
| 599px | 599×569 | 16.1〜79.5% |
| 768px | 560×532 | 16.1〜79.5% |
| 900px〜 | 620×589 | 16.1〜79.5% |

**やってはいけないこと。**

- **高さを `svh` や固定pxにする。** 画面が低いときや幅が広いときだけ枠が平たくなり、
  そこだけバーが切れる。高さは必ず `aspect-ratio` で幅から出すこと。
- **枠を横長にする（幅を広げて高さを据え置く）。** 同じ理由でバーが切れる。
- **PCで幅を620pxより広げる。** 高さが比例して伸びる。全幅1440pxなら高さ1368pxになり、
  コピーが画面2つぶん下へ落ちる。

### 写真を差し替えたときにやること

`object-position` の Y（現在44%）は、この写真の18%〜76%を枠に収めるための値。
**別の写真にしたら、新しい写真で「残したい縦の範囲」を測り直してYを決め直すこと。**
X（50%）は結果に影響しない（左右は全部入るので）。

範囲の測り方は、パーセントの目盛りを重ねた画像を作るのが早い。

```python
from PIL import Image, ImageDraw
im = Image.open('assets/img/hero-coaching.webp').convert('RGB'); w,h = im.size
d = ImageDraw.Draw(im)
for pct in range(0,101,5):
    y = int(h*pct/100); d.line([(0,y),(w,y)], fill=(255,0,0), width=3)
    d.text((8,y+4), f'{pct}%', fill=(255,255,0))
im.save('/tmp/grid.png')
```

### PCではコピーが下がる

写真を大きくするとコピーがそのぶん下へ動く。現在の寸法で「どうせやるなら今から」の
下端が **913px**。1920×1080では画面内に入るが、**1440×900級のノートでは折り返しの
下**になる（ブラウザのバーを引くと実表示は800px前後のため）。

ファーストビューはPCも1カラムという指示があるので、いまはこの形。
コピーを上げたい場合は、写真を小さくする（＝バーが切れる方向）か、
PCだけ2カラム（本家HPと同じ左右振り分け）にするかの二択になる。

### ヘッダーの高さ（`--hd-h`）

ヘッダーは固定配置なので、写真パネルは自力でその高さぶん下がる必要がある。
ところが**ヘッダーの高さは中身の折り返しで69px〜91pxの間で変わり、折り返す幅も
端末の文字サイズ設定で動く**。メディアクエリで決め打ちすると必ずどこかの幅で
「写真がヘッダーの裏に潜る」か「上に白い帯が残る」になる（実際、340pxで切っても
341pxで21px潜った）。

そこで `lp-b.js` が実測値を `--hd-h` に入れ、`.fv` の `padding-top` がそれを使う。
フォントが後から届くと高さが変わるので `document.fonts.ready` でもう一度測っている。
**JSが動かなかったときのフォールバックは94px（いちばん高いとき）。**
写真がヘッダーの裏に潜るより、白い余白が少し残るほうが軽い失敗なので、大きいほうへ倒してある。

- **ファーストビューはPCも1カラム**（2026年8月に2カラムから変更）。
  左右に振り分けると写真だけが右に離れて見えるため、写真パネルを中央に置き、
  その下にコピーを中央寄せで積む形にした。`.fv__in` は `max-width:760px` で行長を抑えている。

## SNS・LINEに貼ったときのサムネ

`assets/img/og-trial.jpg`（1200×630）が `og:image` に指定してある。
**絶対URLでないと読まれない**ので、いま実際に配信されている GitHub Pages を指している。
本番 `dlightgym.com/lp/trial/` へ移したら、`index.html` の `og:image` も
`https://dlightgym.com/lp/trial/assets/img/og-trial.jpg` に直すこと。

作り直すときは、写真を切るのではなく**高さを全部使って幅を人物の中心に寄せる**。
1200×630 に直接切ると、下側の人の顎が切れる。

## 編集時のルール

- **グローバルナビは意図的に置いていません。** LPは離脱を防ぐため、ヘッダーはロゴ＋予約ボタンのみ。
  ナビを足したくなったら、それは本家サイト側の役割です。
- **色・フォント・余白は `lp.css` の `:root` を触る。** 個別要素にハードコードしない。
- 既存クラス（`.section` / `.sec-head` / `.grid` / `.card` / `.split` / `.stage` / `.prog` / `.flow` /
  `.trial-card` / `.ptable` / `.acc` / `.cta-band` / `.notes` / `.trouble` / `.reason` / `.num-card` /
  `.made` / `.rec` / `.assure` / `.sim` / `.campaign` / `.proof`）で
  組めないか確認してから新しいクラスを足す。
- スクロール表示アニメーションは `data-reveal` 属性を付けるだけで効く。
- 日本語の見出しは明朝（`--f-serif`）、本文はゴシック（`--f-sans`）、英字は Jost（`--f-en`）。
- 画像を追加するときは軽量化してから置く（長辺1200px / JPEG品質72 目安、ヒーローのみ1600px）。
  `sips -s format jpeg -s formatOptions 72 -Z 1200 in.jpg --out out.jpg`

## 外部リンク（変更時は全箇所を一括置換）

- 体験トレーニング予約（Square）:
  `https://book.squareup.com/appointments/jqg8slb8u0tfya/location/L7PXPNPQZ5DK2/services/DS5GKGL3V32YYJWJ65FOD5EP`
- 公式LINE: `https://lin.ee/v4NK1TV`
- Instagram: `https://www.instagram.com/dlight_personalgym`
- フッターの「料金システム」「よくあるご質問」等は本家サイト（`https://www.dlightgym.com/○○.html`）へ絶対URLで飛ばしています。

※ヘッドコンディショニングは予約URLが別です。このLPでは扱っていないので、追加する場合は本家の
`CLAUDE.md` を参照して正しいURLを使うこと。

## 掲載内容の出どころ

料金・経歴・資格・FAQ はすべて本家サイト（＝Wix版の実データ）から転記しています。
**勝手に数字や実績を作らないこと。** 不明な値は「公式LINEでお問い合わせください」に寄せます。

- 体験トレーニング 80分 5,000円（税込）／入会金 通常 33,000円（税込）
- 初月のお支払い例は **入会金無料を適用した金額**で計算している。現在は入会金0円のため、初月合計＝月額（37,400円／56,100円）。
  キャンペーンを終了するときは、この2枚のカードを 70,400円／89,100円 に戻し、`sim__after` の「入会金が無料なので〜」の一文も消すこと。
- **ビフォーアフターの写真は本人の許諾済み。**「効果には個人差があり、結果を保証するものではありません」の注記を必ず残すこと（`.ba__note`）。
- ヘッドコンディショニングのビフォーアフター（20代男性・頭のサイズ）は、別サービスで予約URLも違うため**このLPには載せていない**。
- 「1,000名以上」「セッション1万件以上」「ストレッチ継続率9割超え」は代表・奥野個人の実績としてABOUTページに記載のあるもの。ジム全体の実績として書き換えないこと。
- **お客様の声（40代男性）は施主から提供された実際の声**です。本家サイトには未掲載。文面を勝手に脚色しないこと。
- 会員データの円グラフ（年代・入会時の経験・目的）は本家トップと同じ構成比で、**元画像から再現した概算**です。そのため「※構成比は概算です」の注記を必ず残すこと。正確な数値が手に入ったら差し替えます。

## 公開について（未実施）

- `canonical` / `og:url` は `https://www.dlightgym.com/lp/trial/` を仮で入れてあります。
  実際の公開URLが決まったら、`index.html` の以下3か所を実URLに直してください。
  `<link rel="canonical">` / `<meta property="og:url">` / JSON-LD（Service）の `offers.url`
- `og:image` は本家サイトの画像URLを参照しています（`https://www.dlightgym.com/assets/img/hero-home.jpg`）。
  本家と別ドメインで公開する場合は、このLPの `assets/img/hero-home.jpg` の絶対URLに差し替えてください。
- 広告の流入計測をする場合は、`assets/js/analytics.js` 冒頭の `GA_MEASUREMENT_ID` に測定IDを入れるだけで
  予約クリック（`reserve_click` / `generate_lead`）、LINEクリック、FAQ開封、読了率が記録されます。
  空のままなら外部への送信は一切ありません。
